<Window x:Class="DiceRoller.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DnD Dice Roller" Height="320" Width="420">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Enter dice expression (e.g., 1d20+5):" FontWeight="Bold" />
        <TextBox Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding ExpressionText, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Roll mode:" VerticalAlignment="Center" Margin="0,6,0,0" />
        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding RollModes}" SelectedItem="{Binding SelectedRollMode}" Width="140" Margin="0,6,0,0" />

        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="Roll" Command="{Binding RollCommand}" Height="32" Margin="0,10,0,0" />

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Vertical" Margin="0,10,0,0" >
            <TextBlock Text="Result" FontSize="14" FontWeight="Bold" />
            <TextBlock Text="{Binding TotalText}" />
            <TextBlock Text="{Binding RollsText}" TextWrapping="Wrap" />
            <TextBlock Text="{Binding AdvantageText}" TextWrapping="Wrap" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkRed" />
        </StackPanel>
    </Grid>
</Window>
