<Window x:Class="DiceRoller.Wpf.MacroEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Macro Editor" Height="440" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Name" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Text="{Binding MacroName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>

        <TextBlock Grid.Row="2" Text="Category / Tag" FontWeight="Bold"/>
        <TextBox Grid.Row="3" Text="{Binding MacroCategory, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>

        <StackPanel Grid.Row="4" Orientation="Vertical">
            <TextBlock Text="Hit (e.g., 1d20+4)" FontWeight="Bold"/>
            <TextBox Text="{Binding MacroHitExpression, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
            <TextBlock Text="Damage (e.g., 1d6+2)" FontWeight="Bold"/>
            <TextBox Text="{Binding MacroDamageExpression, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8"/>
            <TextBlock Text="Description" FontWeight="Bold"/>
            <TextBox Text="{Binding MacroDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0" AcceptsReturn="True" Height="100" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Save" Command="{Binding SaveMacroCommand}" Width="80" Margin="0,0,8,0"/>
            <Button Content="Clear" Command="{Binding ClearMacroFieldsCommand}" Width="80" Margin="0,0,8,0"/>
            <Button Content="Delete" Command="{Binding DeleteMacroCommand}" Width="80" IsEnabled="{Binding CanDeleteMacro}"/>
        </StackPanel>
    </Grid>
</Window>